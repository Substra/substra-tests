options:
  enable_intermediate_model_removal: False
  enable_model_download: True
  # minikube: True
  # future_timeout: 300
  # future_polling_period: 0.5
  # organization_sync_timeout: 300
organizations:
  - name: 'organization-1'
    msp_id: 'MyOrg1MSP'
    address: 'http://substra-backend.org-1.com'
    user: 'org-1'
    password: 'p@sswr0d44'
    shared_path: '/tmp/org-1/servermedias'
  - name: 'organization-2'
    msp_id: 'MyOrg2MSP'
    address: 'http://substra-backend.org-2.com'
    user: 'org-2'
    password: 'p@sswr0d45'
    shared_path: '/tmp/org-2/servermedias'

mnist_workflow:
  train_samples: 500
  test_samples: 200
